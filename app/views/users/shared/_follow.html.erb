<%= form_for FollowPolymorphic.new, remote: true do |f| %>
    <div><%= hidden_field_tag :id, @user.id %></div>
    <div><%= hidden_field_tag :type, @user.class %></div>
    <%= f.submit "Follow", class: "btn btn-primary btn-block border font-weight-bold" %>
  <% end %>