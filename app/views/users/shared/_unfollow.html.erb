<%= form_for unfollow_user(@user), html: { method: :delete }, remote: true do |f| %>
  <%= f.submit t('users.profile.unfollow'), class: "btn btn-danger btn-sm btn-block border font-weight-bold" %>
<% end %>
